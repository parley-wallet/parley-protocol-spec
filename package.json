{
  "name": "parley-protocol-spec",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "./build.sh",
    "deploy": "./build.sh && wrangler deploy",
    "dev": "./build.sh && wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
